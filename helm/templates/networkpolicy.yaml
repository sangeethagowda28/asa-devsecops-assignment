apiVersion: networking.k8s.io/v1
kind: NetworkPolicy

metadata:
  name: vulntracker-network
  namespace: vulntracker

spec:
  podSelector:
    matchLabels:
      app: vulntracker

  policyTypes:
    - Ingress
    - Egress

  ingress:
    - from:
        - podSelector: {}

  egress:
    - {}